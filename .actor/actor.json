{
    "actorSpecification": 1,
    "name": "amazon-books-scraper",
    "version": "0.1",
    "input": "./.actor/input_schema.json",
    // Remove the "storages" section completely or change to:
    "storages": {
        "dataset": { "type": "memory" }  // Uses in-memory storage instead
    }
}